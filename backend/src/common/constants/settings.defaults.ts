/**
 * Default values for SystemSettings initialization.
 * Client-provided official contact email — stored in DB, not hardcoded in frontend.
 */
export const DEFAULT_COMPANY_EMAIL = 'sales@filter-shielder.com';
