# API Configuration
NEXT_PUBLIC_API_URL=http://localhost:5000/api/v1

# Optional: explicit backend origin used to resolve /uploads/* image paths.
# Set this to the backend origin that serves /uploads in production, without /api.
# Example: NEXT_PUBLIC_UPLOADS_BASE_URL=http://185.216.75.147:5002
NEXT_PUBLIC_UPLOADS_BASE_URL=
