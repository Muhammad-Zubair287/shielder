# API Configuration
NEXT_PUBLIC_API_URL=http://localhost:5000/api/v1

# Optional: explicit backend origin used to resolve /uploads/* image paths.
# Set this in production (e.g. Railway public URL without /api).
# Example: https://your-backend-service.up.railway.app
# NEXT_PUBLIC_UPLOADS_BASE_URL=
